{
  "name": "bridge-to-native",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "react": "^16.3.0-alpha.1",
    "react-native": "^0.54.2"
  }
}
